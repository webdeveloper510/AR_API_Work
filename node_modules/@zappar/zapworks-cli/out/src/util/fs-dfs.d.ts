interface SearchResult {
    locatedInRoot: boolean;
    fileLocation: string | undefined;
}
export default class FileInDirectoryBFS {
    directory: string;
    pathQueue: string[];
    targetFiles: string[];
    constructor(directory: string, targetFiles: string[]);
    private fileExistsInDirectory;
    SearchFiles(maxDepth?: number): Promise<SearchResult>;
    getDirectoryDepth: (dir: string) => number;
    getSubDirectories: (path: string) => Promise<string[]>;
}
export {};
