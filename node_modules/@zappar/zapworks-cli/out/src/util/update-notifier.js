"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const updateNotifier = require("update-notifier");
const chalk_1 = require("chalk");
const pkg = require('../../../package.json');
function checkForUpdates() {
    const notifier = updateNotifier({
        pkg,
        updateCheckInterval: 1000 * 60 * 60 * 24 * 1,
    });
    if (notifier.update) {
        console.log((0, chalk_1.underline)((0, chalk_1.yellow)(`Update available:${notifier.update.latest}`)));
        console.log(`run ${(0, chalk_1.blue)('npm update -g @zappar/zapworks-cli')} to update`);
    }
}
exports.default = checkForUpdates;
