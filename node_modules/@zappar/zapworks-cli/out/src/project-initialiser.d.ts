import { Initialise as InitialiseSettings } from './spec';
export default class CreateTemplate {
    Parsedsettings: InitialiseSettings;
    branch: string;
    constructor(settings: InitialiseSettings, branch?: string);
    clone: () => Promise<void>;
    GetArchiveName: () => string;
    GetURL: () => string;
    parseInput: (str: string) => string;
}
