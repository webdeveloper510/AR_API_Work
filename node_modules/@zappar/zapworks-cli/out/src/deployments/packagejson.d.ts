import { DeploymentProject } from '../spec';
export interface PackageConfig {
    location: string | undefined;
    config: Partial<DeploymentProject>;
}
export declare function getPackageConfig(): Promise<PackageConfig>;
