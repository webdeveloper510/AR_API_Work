/// <reference types="node" />
export declare class Chunk {
    header: string;
    subheader?: string;
    subchunks?: Chunk[];
    data?: Buffer;
    length(): number;
    toBuffer(): Buffer;
    write(buffer: Buffer): void;
    dropChunksWithHeader(header: string): void;
    getChunksWithHeader(header: string): Chunk[];
}
export declare class Riff {
    private _data;
    constructor(data: Buffer);
    static parseChunk(data: Buffer, requiredHeader?: string): Chunk;
}
