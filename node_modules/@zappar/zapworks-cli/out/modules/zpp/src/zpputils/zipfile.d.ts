/// <reference types="node" />
import { IWriteFileSystem, IReadFileSystem } from "./ifilesystem";
export declare class ZIPFileReader implements IReadFileSystem {
    private _filename;
    private _zip;
    constructor(filename: string);
    private _getZIP;
    getFile(filename: string): Promise<Buffer>;
}
export declare class ZIPFileWriter implements IWriteFileSystem {
    private _filename;
    private _zip;
    constructor(filename: string);
    putFile(filename: string, contents: Buffer): Promise<void>;
    save(): Promise<void>;
}
