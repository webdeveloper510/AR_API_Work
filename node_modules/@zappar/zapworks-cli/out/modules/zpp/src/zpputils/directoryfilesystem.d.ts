/// <reference types="node" />
import { IWriteFileSystem, IReadFileSystem } from "./ifilesystem";
export declare class DirectoryFileSystemWriter implements IWriteFileSystem {
    private _baseDir;
    private _filesByPath;
    constructor(dir: string);
    putFile(filename: string, contents: Buffer): Promise<void>;
    private _saveFile;
    save(): Promise<void>;
}
export declare class DirectoryFileSystemReader implements IReadFileSystem {
    private _baseDir;
    constructor(dir: string);
    getFile(filename: string): Promise<Buffer>;
}
